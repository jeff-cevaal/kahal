apiVersion: v1
kind: Service
metadata:
  name: linkding
  namespace: linkding
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: linkding
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9090
  selector:
    app: linkding
  type: LoadBalancer
