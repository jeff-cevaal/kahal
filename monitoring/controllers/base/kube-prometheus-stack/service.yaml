apiVersion: v1
kind: Service
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: kube-prometheus-stack
  type: LoadBalancer
